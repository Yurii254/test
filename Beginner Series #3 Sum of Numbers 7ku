function getSum( a,b )
{
   //Good luck!
    var c = 0;
   
    if (a == b) { 
    c = a;  
    }  
  
    if (a < b) {
       for (var i = a; i <= b; i++){
       c = c + i;
       }
    }
   
    if (b < a) {
       for (var i = b; i <= a; i++){
       c = c + i;
       }
    }
    
return (c);
