function getSum(a,b){
    var c = 0;
   
    if (a == b) return a
   
    if (a < b) {
       for (var i = a; i <= b; i++){
         c += i;
         }
       return (c);
    }
   
    if (b < a) {
       for (var i = b; i <= a; i++){
         c += i;
         } 
       return (c);
    }
 
}
