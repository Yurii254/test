function getSum(a,b) {
  let res = 0;

  if (a == b) return a; 

  if (a < b) for (let i = a; i <= b; i++) res = res + i;

  if (b < a) for (let i = b; i <= a; i++) res = res + i;
  
  return (res);
  }
