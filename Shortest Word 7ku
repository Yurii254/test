function findShort(s1) {

let shortWord = s1.length;
let wordLength = 0;

let arr = s1.split(/\s/);

for (let i = 0; i < arr.length; i++) { //3

  wordLength = arr[i].length;

  if (wordLength < shortWord) shortWord = wordLength;
  
  }//3

return shortWord;
}
