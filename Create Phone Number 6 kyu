function createPhoneNumber(numbers){
  let strOut = '';
  
  for (let i = 0; i < numbers.length;  i++) {
   
    strOut = strOut + numbers[i];
    if (i == 1) strOut = '(' + strOut;
    if (i == 2) strOut =  strOut +')' + ' ';
    if (i == 5) strOut = strOut + '-';
    }

  return strOut;
}
