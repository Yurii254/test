function count (str) { 
  
  // The function code should be here
  let q;
  obj = {};
  
  if (str.length < 1) return obj;
    
  if (str.length > 1) {
  
    for (let i = 0; i < str.length; i++){
      q = str[i];
      obj[q] = (q in obj) ? obj[q] + 1 : 1;
      }
      
    return obj;
  }
  
}
