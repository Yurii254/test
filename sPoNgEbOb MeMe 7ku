 function spongeMeme(str) {
  
  var str2=[];

  for (var i = 0; i < str.length; i++ ){
          
          if (i % 2 === 0) {
          str2[i] = str[i].toUpperCase();  
          } else {
          str2[i] = str[i].toLowerCase();
          }
   }
   return str2.join('');
}
